import { FilterPartPipe } from './filter-part.pipe';

describe('FilterPartPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPartPipe();
    expect(pipe).toBeTruthy();
  });
});
