import { FilterPartnamePipe } from './filter-partname.pipe';

describe('FilterPartnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPartnamePipe();
    expect(pipe).toBeTruthy();
  });
});
